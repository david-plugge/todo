---
name: verify-in-browser
description: Use the Chrome DevTools MCP to verify behavior in a real browser — navigating pages, interacting, taking snapshots/screenshots, and checking the console and network. Use whenever you need to confirm a change works in the browser, not just that it typechecks or builds.
---

# Verify in the browser

When you need to confirm a change actually works in a browser — not just that it typechecks or
builds — drive it with the **Chrome DevTools MCP** instead of assuming.
